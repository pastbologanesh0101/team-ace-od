# Supabase — from Project Settings -> API
NEXT_PUBLIC_SUPABASE_URL=https://wegunpnmunoroutgrtyg.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...            # the "anon public" key (browser-safe)
SUPABASE_SERVICE_ROLE_KEY=eyJ...                # the "service_role" key (SERVER ONLY — set in Vercel, never commit)

# Comma-separated list of emails allowed into the admin view
ADMIN_EMAILS=pranav27062007@gmail.com

# The site's own URL, used for magic-link redirects
NEXT_PUBLIC_SITE_URL=http://localhost:3000
